# Generated by XcodeGen from project.yml — never edit by hand, never commit.
Mob.xcodeproj/
Mob/Info.plist
Mob/Mob.entitlements

# The on-device model is ~2 GB. It belongs on your machine, not in git —
# download it from Hugging Face and drop it in mobile/Mob/ as
# mob-model.gguf (see README.md).
*.gguf

# Xcode / SPM local state
xcuserdata/
*.xcworkspace/xcshareddata/swiftpm/.build/
.swiftpm/
DerivedData/
